# Лабораторна робота №7  
**Тема:** Серіалізація та десеріалізація об’єктів. Збереження та читання колекцій у форматах CSV та JSON.  
**Проєкт:** Music Library (Список музичних треків)  
**Мова:** C# (.NET)

---

## Мета роботи
Навчитися виконувати серіалізацію та десеріалізацію колекцій об’єктів у різні формати файлів  
(*CSV та JSON*), а також розширити функціонал застосунку, додавши:

- методи збереження/читання списку об’єктів у файли,
- обробку помилкових рядків CSV,
- підменю вибору формату,
- робочі `static`-методи класу,
- оновлене текстове меню користувача,
- юніт-тести для нового функціоналу.

---

## Предметна область: **Музичні треки**
Кожен об’єкт — це **MusicTrack**, що містить:

- `Title` — назва треку  
- `Artist` — виконавець  
- `Duration` — тривалість (у секундах)  
- `Genre` — музичний жанр  

Колекція треків зберігається у `List<MusicTrack>`.

---

## Структура проєкту

/ProjectRoot
├── MusicTrack.cs # Клас музичного треку (з повним набором static-методів)
├── MusicManager.cs # Логіка роботи з колекцією (CRUD, CSV, JSON)
├── Program.cs # Консольне меню
├── tracks.csv # Автоматично створюється після збереження
├── tracks.json # Автоматично створюється після збереження
└── README.md # Опис проєкту

yaml
Копіювати код

---

## Функціонал програми

### 1. Робота з треками (основні дії)
Меню дозволяє:

- додати трек;
- переглянути всі треки;
- знайти трек за назвою;
- відсортувати колекцію за назвою;
- видалити трек;
- очистити список.

---

### 2. Серіалізація

#### **CSV**
- запис усієї колекції в `tracks.csv`
- перший рядок — заголовок
- при читанні пропускаються:
  - рядки з помилками,
  - неправильні типи,
  - неповні записи

#### **JSON**
- серіалізація всієї колекції цілком
- форматування з відступами
- десеріалізовані об’єкти додаються до існуючих

---

### 3. Робочі статичні методи класу `MusicTrack`

- `NormalizeAllTitles(List<MusicTrack>)` — приводить усі назви до нормального формату  
- `FindByGenre(List<MusicTrack>, string)` — повертає всі треки певного жанру  
- `CalculateAverageDuration(List<MusicTrack>)` — середня тривалість колекції  
- `NormalizeTitle(string)` — нормалізація однієї назви  
- `FormatDuration(int)` — перетворення секунд у формат `MM:SS`  
- `IsValidGenre(string)` — перевірка жанру  

Статичні методи — **реальні й робочі**, а не демонстраційні.

---

## 🖥 Консольне меню (оновлене)

1 – Додати трек
2 – Переглянути всі треки
3 – Знайти трек
4 – Відсортувати треки
5 – Видалити трек
6 – Операції над колекцією (static)
7 – Зберегти у файл
8 – Зчитати з файлу
9 – Очистити колекцію
0 – Вихід

makefile
Копіювати код

### Підменю пунктів 7 та 8:

**Збереження:**
1 – у CSV
2 – у JSON

makefile
Копіювати код

**Завантаження:**
1 – з CSV
2 – з JSON
